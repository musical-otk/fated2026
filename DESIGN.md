# 뮤지컬 〈FATED〉 관극 다이어리 — 설계서

작성일 2026-09-22 · 베이스 코드 `D:\workspace\ikkisup2026` (이끼숲)

> **이 문서는 검토용이다. 확정 전에 코드를 짜지 않는다.**

---

## 1. 개요

트로이 전쟁의 아킬레우스·파트로클로스·헥토르 세 사람을 다룬 3인극 뮤지컬
(2026.11.03 ~ 2027.01.24, 링크아트센터드림 드림2관)용 관극 기록 PWA.

베이스 앱이 제공하는 기능(승계): 홈 / 캘린더 / 쿠폰·도장 / 통계 / 설정.
저장소는 브라우저 localStorage — 기기 로컬, 기기 간 동기화 없음.

### 베이스 선정 근거

세 후보의 기능 보유 현황을 실제로 대조했다.

| 기능 | widerstand2026 | **ikkisup2026** | oi2026 |
| --- | --- | --- | --- |
| `FLOOR_ZONES` / `SEAT_UNAVAILABLE` | ✗ | **O** | O |
| `defaultMilestones()` | ✗ | **O** | O |
| 허브 딥링크 `openFromHub()` | O | **O** | **✗** |
| 캐스팅 재동기화 `syncCastingFromSchedule()` | O | **O** | **✗** |
| 양도 처리 `isTransferred` | O | **O** | **✗** |

**ikkisup2026 이 유일한 상위집합**이라 이것을 베이스로 쓴다.
스킬 문서는 베이스를 `widerstand2026` 으로 적고 있으나, 그 이후 이끼숲에서
좌석 구역·마일스톤 일반화가 들어갔다. **스킬 문서를 갱신해야 한다** (§9).

`oi2026`(오셀로와 이아고)은 FATED 와 같은 **단층 극장**이라 층 탭 제거 패턴만 참고한다.
허브 연동 3종이 통째로 빠져 있어 베이스로는 쓸 수 없다 — pitfalls §17·§19 의 실제 사례.

---

## 2. 자료 확보 현황

### 2.1 확인 완료

| 항목 | 값 | 출처 |
| --- | --- | --- |
| 공연 기간 | 2026.11.03(화) ~ 2027.01.24(일) | INFORMATION 카드 |
| 극장 | 링크아트센터드림 드림2관 | INFORMATION 카드 |
| 공연시간 | 화·수·목·금 20:00 / 토 15:00, 19:00 / 일·공휴일 14:00, 18:00 (**월 공연 없음**) | INFORMATION 카드 |
| 러닝타임 | 100분 | INFORMATION 카드 |
| 관람등급 | 8세 이상 (2019년생 포함 이전 출생자) | INFORMATION 카드 |
| 티켓가격 | **전석 66,000원** (단일 등급 R석) | INFORMATION 카드 |
| 예매처 | 티켓링크 | 티켓오픈 안내 |
| 1차 티켓오픈 | 2026.09.21(월) 14:00 — 오픈 회차 11/3~11/22 | 티켓오픈 안내 |
| 좌석 | A~L 12열 **211석** (판매 197 / 미판매 14) | SEAT PLAN |
| 예매문의 | 스토리정원 070-4210-0822 | INFORMATION 카드 |
| 공식 X | [@Knowerscompany](https://x.com/Knowerscompany) | — |

**배역 3종 / 배우 9명**

| 키 | 배역 | 배우 |
| --- | --- | --- |
| `achilles` | 아킬레우스 | 박규원 · 신주협 · 박준형 |
| `patroclus` | 파트로클로스 | 반정모 · 조은성 · 김재한 |
| `hector` | 헥토르 | 이동수 · 조민호 · 신수빈 |

**캐스팅 스케줄** — 25회차 (11/3~11/22), 1차 오픈분 전량

판독 검증 — 배우별 회차 수 합계가 세 배역 모두 정확히 **25** 로 맞는다.
전사 중 한 칸이라도 밀렸다면 이 대칭이 깨진다.

| 아킬레우스 | 회차 | 파트로클로스 | 회차 | 헥토르 | 회차 |
| --- | ---: | --- | ---: | --- | ---: |
| 박규원 | 9 | 반정모 | 9 | 조민호 | 9 |
| 신주협 | 8 | 김재한 | 9 | 이동수 | 8 |
| 박준형 | 8 | 조은성 | 7 | 신수빈 | 8 |
| **합** | **25** | **합** | **25** | **합** | **25** |

회차 수가 균등하지 않은 것은 오류가 아니다 (오셀로와 이아고도 12~17회로 편차가 있었다).

**첫공** — 11/3 박규원·반정모·이동수 / 11/4 신주협·김재한·신수빈 / 11/5 박준형·조은성·조민호
(공식 표에 `첫공` 뱃지로 명시. 9명 전원 프리뷰 3일에 첫공을 치른다.)

**MANIA CARD (재관람 카드)** — 7칸

| 누적 | 보상 |
| ---: | --- |
| 3회 | 포토카드 9종 |
| 7회 | 지정 폴라로이드 사진 1장 + 실황 OST |

적립 규칙 (원문 판독):

- 재관람 카드는 **유료 관람 2회차부터 발급** 가능
- 적립은 **당일** 유료 관람 회차에 한함. **동일 회차·지난 회차 적립 불가**
- 당일 카드 미지참 시 적립 불가
- 티켓 확인 시 유료 티켓 **뒷면에 확인 도장 날인**, 날인된 티켓 재사용 불가
- **여러 장의 카드 합산 불가**, 분실·훼손 재발급 불가
- 발급·날인은 MD부스에서 공연 시작 1시간 전 ~ 시작 10분 전, 종료 후 10분까지
  (2회 공연 시 해당 회차 MD부스 운영 시간에만 적립 가능)

**할인 5종**

| 할인 | 율 | 매수 | 조건 |
| --- | ---: | --- | --- |
| 프리뷰 할인 | 50% | 1인 2매 | 11/3(화)~11/8(일) 회차 |
| 1차 조기예매 할인 | 20% | 1인 2매 | 9/21~9/27 예매분, 11/3~11/22 회차 |
| 재관람 할인 | 25% | 1인 1매 | 2026 FATED 유료 티켓 소지자 |
| 학생 할인 | 20% | 본인 1인 1매 | 2008~2019년생 |
| 복지 할인 | 50% | 장애인 1인 2매 / 유공자 본인 1인 1매 | 증빙 지참 |

재관람 할인 세부: 예매내역서·모바일 예매내역·티켓 사진·동일 회차 티켓으로 증빙 불가.
**재관람 도장이 날인된 증빙 완료 티켓은 중복 사용 불가** (→ §7 열린 질문 1).

**팔레트** (공식 이미지에서 샘플링)

| 역할 | 값 |
| --- | --- |
| 배경 딥네이비 | `#12161F` |
| 카드/헤더 네이비 | `#141A26` |
| 좌석(R석) 네이비 | `#082247` |
| 골드 액센트 | `#B99A6B` (포스터 각인 `#806848`~`#907050`) |
| 크림 본문 | `#CCC4B9` → 밝은 쪽 `#E8E2D6` |
| 브론즈(미판매석) | `#96816C` |

### 2.2 미확보

| 항목 | 상태 | 사유 |
| --- | --- | --- |
| 11/23 이후 캐스팅 스케줄 | 미공개 | 1차 오픈이 11/22 까지만. 2차 오픈 시 재수집 |
| 특전·이벤트 (`events.json`) | **없음** | 계정에 특전 안내 게시물이 아직 없다 |
| 창작진 (연출·작곡·극본) | 미확보 | 계정에 크레딧 게시물 없음. 웹 검색 미색인 |
| 좌석 등급 구분 | **불필요** | 전석 66,000원 단일 등급 |
| 공휴일 회차 (12/25, 1/1 등) | 미확보 | 2차 오픈 안내에 포함될 예정 |

### 2.3 수집 경로 기록

| 경로 | 결과 |
| --- | --- |
| X 비로그인 (`WebFetch`) | **HTTP 402** — 문서화된 그대로 |
| 기존 쿠키 재사용 (9/6자) | **`Could not authenticate you`** — 파일상 만료일은 2027년이나 서버가 세션 무효화 |
| 쿠키 재발급 후 | ✅ 전량 수집 |
| 웹 검색 (US 색인) | ❌ "페이티드"·"Knowers Company" 모두 미색인 |
| PlayDB (`playdb.co.kr`) | ❌ 이 네트워크에서 DNS 미해석 (`ENOTFOUND`) |
| 티켓링크 공지 `notice/65965` | ❌ SPA — 본문 "티켓링크" 한 단어만 |
| `from:Knowerscompany 페이티드` 검색 | 7장 — **본문에 작품명을 안 쓰는 게시물을 놓친다** |
| **미디어 탭 전체** | ✅ 21장 / 15건 — 계정이 페이티드 전용이라 안전 |

**새로 겪은 실패 — gallery-dl 버전** (스킬 문서에 없던 것)

```
[twitter][error] HttpError: '404 Not Found' for
  'https://abs.twimg.com/responsive-web/client-web/ondemand.s.Nonea.js'
```

1.32.11 이 X 의 변경을 못 따라가 죽었다. `pip install --upgrade gallery-dl` (→1.32.13) 로
해소됐고, **그제서야 진짜 원인인 쿠키 만료가 드러났다.** 오류가 겹치면 바깥 것부터 걷어내야 한다.

---

## 3. 데이터 모델

### 3.1 `schedule.json` — 25건

```jsonc
[{ "date": "2026-11-03", "time": "20:00",
   "achilles": "박규원", "patroclus": "반정모", "hector": "이동수" }]
```

하루 2회차(토·일·11/18)는 같은 `date` 로 두 항목.

### 3.2 `events.json` — **빈 배열로 시작**

특전 안내가 아직 없다. 게시되면 재수집해 채운다 (`archive.sqlite3` 가 증분 처리).

### 3.3 `seating.json`

```jsonc
{ "floors": { "1층": { "rows": {
    "A": { "l": [1,10], "r": [11,20] },  "B": { "l": [1,10], "r": [11,19] },
    "C": { "l": [1,10], "r": [11,20] },  "D": { "l": [1,10], "r": [11,19] },
    "E": { "l": [1,7],  "r": [8,17]  },  "F": { "l": [1,7],  "r": [8,17]  },
    "G": { "l": [1,7],  "r": [8,17]  },  "H": { "l": [1,9],  "r": [10,19] },
    "I": { "l": [1,9],  "r": [10,19] },  "J": { "l": [1,9],  "r": [10,19] },
    "K": { "l": [1,9],  "r": [10,18] },  "L": { "l": [1,7],  "r": null    }
  } } },
  "unavailable": { "1층": { "A":[1,2,19,20], "B":[1,19], "C":[1], "L":[1,2,3,4,5,6,7] } } }
```

### 3.4 `show-info.json`

작품명·극장·기간·러닝타임·등급·가격·배역·요일별 공연시간을 한 곳에 모아
`index.html` 하드코딩과 `validate.py` 검사의 단일 출처로 쓴다.

---

## 4. 베이스 코드 개조 지점

`ikkisup2026/index.html` (4123줄) 기준 **실제 줄 번호**.

| # | 줄 | 대상 | 변경 |
| ---: | ---: | --- | --- |
| 1 | 6 | `<title>` | ⚔️ FATED |
| 2 | 8 | `<meta theme-color>` | `#12161F` — **manifest 의 `theme_color` 와 동일값 필수** |
| 3 | 11 | `apple-mobile-web-app-title` | FATED |
| 4 | 236 | `.next-show-card::before` | ⚔️ |
| 5 | 44·305·341 | 홈 `page-title` | ⚔️ FATED |
| 6 | 1061 | `APP_PREFIX` | `'ikki_'` → **`'fated_'`** (오리진 충돌 방지, pitfalls §11) |
| 7 | 1070 | `indexedDB.open` | `'fated_photos'` |
| 8 | 1238 | 백업 파일명 | `FATED_backup_...` |
| 9 | 1328 | `defaultMilestones()` | **3회 포토카드 9종 / 7회 폴라로이드+OST** |
| 10 | 1349 | `totalSlots` 기본값 | 8 → **7** (3곳 전부) |
| 11 | 1373 | `SEAT_PLAN` | 단층 A~L (§4.4) |
| 12 | 1407 | `SEAT_UNAVAILABLE` | A1,A2,A19,A20 / B1,B19 / C1 / L1~7 |
| 13 | 1414 | `FLOOR_ZONES` | `{ '1층': { l:10, m:0, r:10, align:'edge' } }` |
| 14 | 1419·1425 | `floorWidth` / `floorLeadPx` | 단층이라 `floorLeadPx` 는 항상 0 — 그대로 둠 |
| 15 | 1442 | `DISCOUNT_COUPON_MAP` | **`{}`** (§7 열린 질문 3) |
| 16 | 1446 | `DISCOUNT_LABEL` | 할인 5종 + 정가 |
| 17 | 1555 | `ROLES` | 5 → **3** 배역 |
| 18 | 1650 | `FLOOR_ROWS` | `{ '1층': ['A'..'L'] }` |
| 19 | 2192 | `buildMiniSeat` | `templates/seat-render.js` 로 교체 — **놓치기 쉬움** |
| 20 | 2473 | `doXSearch()` | 검색 URL 을 `@Knowerscompany` 로 |
| 21 | 3099 | `renderHeatmap` | 위와 **같은 상수** 공유 (pitfalls §2) |
| 22 | 3393 | `COUPON_PACKS` | **`{}`** — 특전 미공개 |
| 23 | 161·162 | `.heatmap-tab` CSS + 층 탭 HTML | **단층이므로 삭제** (oi2026 패턴) |
| 24 | — | `manifest.json` | 이름·`start_url`·`scope`·색상 |
| 25 | — | `sw.js` | `CACHE_NAME`·`BASE` |
| 26 | — | `guide.html` | 제목·극장명·도장판 명칭(MANIA CARD)·칸수·혜택 |
| 27 | — | 아이콘 | 메인 포스터로 교체 (`make_icons.py`) |
| 28 | — | CSS `:root` / `[data-theme=dark]` / `--seat1~5` | §2.1 팔레트 |

### 4.1 배역 5 → 3 축소

`ROLES` 만 줄이면 HTML 정적 요소가 남아 `getElementById` 가 `null` → 저장이 터진다.
손으로 지울 곳: 캘린더 배역 탭 · 페어 필터 탭 · 기록 폼 `form-group` ·
`saveRecord()` 필드 · `calFilter` 초기값 · `SHORT` 약칭 맵.

> ⚠️ **기록 폼의 `#rec-transferred` / `#rec-transfer-to` 도 같은 `form-group` 이다.**
> 배역 폼을 지우다 같이 날아가면 **에러 없이 숫자만 조용히 틀린다** (pitfalls §19).
> 지운 직후 `check_html.py` 7번 검사를 돌린다.

### 4.2 필수 배역

**없앤다.** 3인극이라 한 배역만 보고 기록할 일이 없고, 베이스 선례(이끼숲·오셀로와 이아고)도
모두 필수 없음이다. 날짜·시간만 필수.

### 4.3 할인·쿠폰·도장

**실물 할인권이 이 작품에는 없다.** MANIA CARD 보상이 포토카드·폴라로이드·OST 라
`DISCOUNT_COUPON_MAP` 이 빌 수밖에 없고, 쿠폰 목록 화면이 항상 빈 상태가 된다.
→ 탭은 유지하고 빈 상태 안내를 넣는다 (§7 결정 3).

도장판은 **7칸, 마일스톤 2단계(3회·7회), 선택형 아님** → `choiceRequired` 를 넣지 않으면
관련 UI 가 자동 비활성화된다. 관련 코드를 지우려 들지 않는다 (pitfalls §13).

#### 차액 지불 (`paidDiff`) — **반드시 남긴다**

베이스에 `paidDiff` 가 11곳 있다 (폼 `#rec-paid-diff`, 저장 필드, 목록의 `(차액지불)` 뱃지,
쿠폰 집계 제외 조건). **이 작품에도 그대로 적용된다** — 공식 할인 안내의
재관람·학생·복지 3종 모두 "증빙 미지참 시 정가 대비 차액 지불"이라고 적고 있다.

> ⚠️ `paidDiff` 의 용례 대부분이 `DISCOUNT_COUPON_MAP` 과 얽혀 있다.
> 그 맵을 비우면서 **관련 코드를 죽은 코드로 보고 지우기 쉽다.**
> 폼 체크박스·저장 필드·목록 뱃지(2141줄)는 쿠폰과 무관하게 살아 있어야 한다.

반면 `eungBingPassUsed` 는 이끼숲 전용(응빙 패스)이라 **제거 대상**이다.
`paidDiff` 와 나란히 쓰이는 자리가 많아 함께 지우지 않도록 주의한다.

### 4.6 비공식 운영 규칙

공식 공지에 없거나 공지와 다른, 사용자가 확정한 규칙.

| 규칙 | 공식 문구 | 확정 |
| --- | --- | --- |
| 도장 적립 시작 | "유료 관람 **2회차**부터 발급 가능" | **1회차부터 1개씩** (7칸 = 7회) |
| 도장 ↔ 재관람 할인 증빙 | 같은 도장으로 읽힘 | **별개로 처리** |

근거와 판단 경위는 §7. **공식 문구와 다르다는 이유로 되돌리지 말 것.**

### 4.4 좌석 구성

```
FLOOR_ZONES = { '1층': { l: 10, m: 0, r: 10, align: 'edge' } }
```

`align:'edge'` 근거 — 확대 판독 결과 좌 구역은 모든 열이 통로 쪽(오른쪽)에 붙어 끝나고,
우 구역은 모든 열이 통로 쪽(왼쪽)에서 시작한다. 열마다 엇갈리는 `center` 가 아니다.

| 열 | 좌 | 우 | 계 | | 열 | 좌 | 우 | 계 |
| --- | --- | --- | ---: | --- | --- | --- | --- | ---: |
| A | 1–10 | 11–20 | 20 | | G | 1–7 | 8–17 | 17 |
| B | 1–10 | 11–19 | 19 | | H | 1–9 | 10–19 | 19 |
| C | 1–10 | 11–20 | 20 | | I | 1–9 | 10–19 | 19 |
| D | 1–10 | 11–19 | 19 | | J | 1–9 | 10–19 | 19 |
| E | 1–7 | 8–17 | 17 | | K | 1–9 | 10–18 | 18 |
| F | 1–7 | 8–17 | 17 | | L | 1–7 | — | 7 |

**판독 합계 = 211석. 미판매 14석(A1·A2·A19·A20, B1·B19, C1, L열 전체) → 판매 197석.**

극장 공표 객석수와의 대조는 **못 했다** — PlayDB·검색 모두 이 네트워크에서 막혔다.
스킬 문서 기준으로 **공식 계정 좌석도가 기준**이므로 판독값을 그대로 쓴다.
공표치를 아시면 대조하겠다 (§7 열린 질문 5).

### 4.5 공연시간 — 고지와 스케줄이 어긋나는 지점

INFORMATION 은 "화수목금 8시"라고만 적었으나, 실제 캐스팅 스케줄에는
**11/18(수) 16:00 · 20:00 2회 공연**이 있다. 고지는 대표값이고 스케줄이 실제다.

`validate.py` 의 요일별 시간표 검사는 `show-info.json` 선언값을 쓰되
**11/18 을 예외로 등록**해야 한다. 그러지 않으면 정상 데이터가 실패로 잡힌다.

구현 시 `derive_showtimes.py` 로 `schedule.json` 에서 역산해 재확인한다.

---

## 5. 자료 수집 파이프라인

| 구성요소 | 상태 |
| --- | --- |
| Python / gallery-dl | ✅ **1.32.13 으로 업그레이드함** (1.32.11 은 X 접근 불가) |
| `cookies.txt` | ✅ 재발급 완료 · `D:\workspace\fated-collector\config\` · `.gitignore` 등록됨 |
| 수집 실적 | ✅ 트윗 15건 / 이미지 21장 / 필독 3건 6장 **전량 판독 완료** |

재수집 (2차 오픈 시):

```powershell
cd D:\workspace\fated-collector
.\collect.ps1 -Url "https://x.com/Knowerscompany/media" -Limit 400
```

`archive.sqlite3` 가 받은 것을 기억하므로 **새 게시물만** 내려온다.

> 수집 폴더는 앱 저장소와 분리돼 있다. 쿠키·이미지는 커밋되지 않는다.

---

## 6. 작업 순서

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| 1 | 환경 준비 | ✅ |
| 2 | X 수집 (15건 / 21장) | ✅ |
| 3 | 이미지 판독 (필독 6장 전량) | ✅ |
| 4 | 설계서 검토 | ✅ 2026-09-22 확정 |
| 5 | 데이터 파일 4종 + `validate.py` | ✅ |
| 6 | `index.html` 개조 | ✅ |
| 7 | 3계층 테스트 루프 | ✅ **38/38 통과** |
| 8 | 아이콘·가이드·스크린샷 | ✅ |
| 9 | GitHub Pages 배포 | ✅ |

**임계 경로는 6단계**였다. 예상대로 배역 축소(§4.1)와 좌석 렌더(§4.4)가 위험했지만,
실제로 회귀를 낸 것은 **쿠폰 체계 제거**였다 (§6.2).

### 6.1 검사 결과

```
L1 데이터 검증 — 통과
  공연 회차 25건 / 이벤트 0건
  좌석 211석 · 미판매 14석 · 실판매 197석
  월요일 공연: 없음
  아킬레우스 박규원 9 · 박준형 8 · 신주협 8
  파트로클로스 김재한 9 · 반정모 9 · 조은성 7
  헥토르     신수빈 8 · 이동수 8 · 조민호 9
  [경고] 2026-11-18 16:00 수요일 예상 시간 불일치 / 1회만 나타나는 시간 [16:00]
         → 둘 다 §4.5 의 정상 경고다. expectedShowtimes 에 16:00 을 넣어 지우지 않는다.

L2 정적 점검 — 통과 (index.html · guide.html · manifest.json · sw.js)
L3 로직 셀프테스트 — RESULT 38 passed, 0 failed
```

### 6.2 구현하며 드러난 것

**좌석 렌더는 교체할 필요가 없었다.** 설계서는 `templates/seat-render.js` 로 갈아끼운다고
적었지만, `ikkisup2026` 의 `renderHeatmap` 과 `buildMiniSeat` 은 이미 `FLOOR_ZONES`·
`floorLeadPx` 를 공유하고 edge 정렬을 구현하고 있었다. **상수만 바꾸면 됐다.**
스크린샷으로 두 좌석도가 일치하는 것을 확인했다 (C열 12번이 양쪽 모두 우구역 두 번째).

**진짜 위험은 쿠폰 체계였다.** 이 작품엔 쿠폰이 하나도 없어 (실물 할인권 2종 ·
증빙패스권 · 더블적립권이 전부 이끼숲 전용) 관련 UI 를 걷어내야 했는데, 그 과정에서
**사진 뷰어(`#photo-viewer`)가 쿠폰 패널 사이에 끼어 있어 함께 삭제됐다.**
`check_html.py` 2번 검사(없는 id 참조)가 잡아내 복원했다.
→ 블록 삭제는 반드시 "잘라낸 내용"을 단언해야 한다. (§9 에 반영)

**검사기 자체에 버그가 셋 있었다.** §9 참조.

**화면을 봐야만 찾히는 것이 둘 있었다** (pitfalls §1 그대로):

| 놓칠 뻔한 것 | 왜 검사에 안 걸렸나 | 어떻게 찾았나 |
| --- | --- | --- |
| 다크 테마 D-day 그라디언트 `#123A3E→#0C282C` | 라이트 테마만 바꿨고 색상이 블랙리스트에 없었다 | **다크모드 스크린샷** |
| MANIA CARD 혜택 안내가 `4회:40% · 6회:50% · 8회:폴라로이드` | 전부 숫자라 어떤 패턴에도 안 걸린다 | 화면 전수 확인 |
| 도장판 빈 상태 `8칸 · 4회:40% …` | 위와 같음 | 화면 전수 확인 |
| 홈 "쿠폰 현황" 빈 카드 | 오류가 아니라 그냥 비어 있었다 | 스크린샷 |

마지막 둘은 고치면서 **마일스톤 배열 기반으로 일반화**했고, 칸수는 `DEFAULT_SLOTS`
한 곳으로 모아 `index.html`·`selftest.html`·`_shots.html` 이 같은 값을 쓰게 했다.

---

## 7. 결정 사항

2026-09-22 사용자 검토에서 전부 확정됐다. 열린 질문 없음.

### 1. 도장과 재관람 할인은 **별개로 처리** ✅

공식 문구상 같은 도장으로 읽혔으나 (카드: "티켓 뒷면에 확인 도장 날인, 날인된 티켓 재사용 불가" /
할인: "재관람 도장이 날인된 증빙 완료 티켓 중복 사용 불가"), **각각 따로 계산**한다.

근거 — 둘을 묶었다가 틀리면 적립 가능 회차가 부당하게 줄어든다. 별개로 두면 틀려도 손해가 없고,
현장 확인 후 고치기도 쉽다. 앱 로직도 단순해진다.

→ 회차 기록에 "할인 증빙으로 사용함" 플래그를 두지 않는다. 도장 계산은 관람 회차만 본다.

### 2. 도장은 **1회차부터 1개씩** ✅ — 공식 문구와 다름

공식 안내는 "재관람 카드는 유료 관람 2회차부터 발급 가능"이고, 판독 시점의 해석은
"1회차 도장 없음 → 7칸 채우려면 총 8회"였다. **사용자 결정으로 1회차부터 1개씩 센다.**

| | 판독 해석 | **확정** |
| --- | --- | --- |
| 1회차 | 도장 없음 | **도장 1개** |
| 7칸 완성 | 총 8회 관람 | **총 7회 관람** |

> **이것은 §4.6 비공식 운영 규칙이다.** 공식 고지와 다르므로, 나중에 코드를 보고
> "공식 문구와 안 맞는다"며 되돌리지 말 것. 현장 운영을 아는 사용자의 판단이다.

→ `defaultMilestones()` 는 3회·7회 그대로, 도장 적립은 관람 1회차부터 1개.

### 3. 할인권 탭은 **유지 + 빈 상태 안내** ✅

실물 할인권이 없어 목록이 항상 비지만, 탭을 남기고 빈 상태 문구를 넣는다.
2차 특전에서 쿠폰이 생기면 `COUPON_PACKS` 만 채워 즉시 대응할 수 있다.

### 4. 저장소 `musical-otk/fated2026` ✅

`https://musical-otk.github.io/fated2026/` · `APP_PREFIX = 'fated_'` ·
IndexedDB `fated_photos` · 허브 프리셋 `recordsKey` 도 동일하게 맞춘다.

### 설계 단계에서 해소된 것

- *베이스 코드 선정* → `ikkisup2026`. 세 후보 기능 대조 결과 유일한 상위집합 (§1).
- *필수 배역* → 없앤다. 3인극 + 베이스 선례 (§4.2).
- *좌석 정렬 모드* → `edge`. 확대 판독으로 좌/우 구역 정렬 방향 확인 (§4.4).
- *수집 범위* → 미디어 탭 전체. 계정이 페이티드 전용임을 사용자 확인 + 검색 7장 vs 전체 21장 (§2.3).

### 남은 확인 (진행을 막지 않음)

- **링크아트센터드림 드림2관 공표 객석수** — 판독 211석의 대조용. 불일치해도 **공식 좌석도를 따른다.**
- **앱 표기 제목** — 공식 표기가 전부 영문이라 `FATED` 로 통일하고 한글 "페이티드"는 쓰지 않는다.
  다르게 원하시면 말씀해 주시면 바꾼다.

---

## 8. 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| **스케줄이 11/22 까지뿐** | 12~1월 회차를 앱에서 못 본다 | 2차 오픈 시 재수집 → `schedule.json` 교체. `syncCastingFromSchedule()` 이 **오늘 이후** 기록만 갱신하므로 지난 기록은 안전 |
| **허브 연동 누락** | 딥링크가 조용히 죽고, 양도 처리가 빠지면 숫자만 틀림 | `ikkisup2026` 베이스라 이미 있음 + `check_html.py` 6·7번 검사 |
| 쿠키 재만료 | 다음 수집 때 또 막힘 | 실패 시 `Could not authenticate you` 로 즉시 식별 가능. 재발급 절차 §2.3 |
| gallery-dl 노후화 | X 변경 시 정체 모를 404 | **수집 전 `pip install --upgrade gallery-dl` 를 기본 절차로** (스킬에 반영) |
| 특전 미공개 | `events.json` 빈 채로 배포 | 빈 상태 화면을 반드시 눈으로 확인 (pitfalls §14) |
| 좌석 공표치 미대조 | 판독 오류를 못 걸러냄 | A~L 12열 구조상 자체 정합. 공표치 확보 시 재대조 |
| **스킬 문서가 베이스를 구버전으로 지목** | 다음 작품에서 또 기능 빠진 베이스를 고름 | `retarget.md` 의 베이스 지정 갱신 (§9) |

---

## 9. 스킬 문서에 반영할 것

이번에 새로 드러난 것들. 배포 후 `~/.claude/skills/musical-diary` 에 반영한다.

### 수집

1. **`references/collect.md`** — gallery-dl 버전 노후화 실패 유형 추가.
   `404 ondemand.s.*.js` → 업그레이드로 해소. **수집 전 업그레이드를 기본 절차로.**
2. **`references/collect.md`** — 쿠키가 파일상 만료 전이어도 서버가 무효화할 수 있다.
   `Could not authenticate you` 가 그 신호. 만료일 확인만으로 유효 판정하면 안 된다.

### 베이스

3. **`references/retarget.md`** — 베이스를 `widerstand2026` → `ikkisup2026` 으로 갱신.
   세 프로젝트 기능 대조표(§1)를 그대로 싣는다.
4. **`references/retarget.md`** — 좌석 렌더 교체 지침 완화. `ikkisup2026` 기준이면
   `renderHeatmap`·`buildMiniSeat` 이 이미 상수를 공유하므로 **상수만 바꾸면 된다.**
   `templates/seat-render.js` 는 `widerstand2026` 에서 출발할 때만 필요하다.

### 검사기 버그 — 셋 다 이번에 고쳤고 스킬에 반영해야 한다

5. **`templates/validate.py`** — 경고 문구에 `—`(em-dash)가 들어가면
   `UnicodeEncodeError: 'cp949' codec can't encode character '—'` 로 **죽는다.**
   PowerShell 5.1 기본 콘솔이 cp949 다. `print` 를 감싸거나 `sys.stdout.reconfigure(encoding='utf-8')` 필요.
   (현재는 `$env:PYTHONIOENCODING='utf-8'` 로 우회 중)
6. **`check_html.py`** — 이끼숲에 실려 있던 사본이 기대 배역 키를 **하드코딩**하고 있었다.
   스킬의 정식 사본은 `check-config.json` 을 읽으니, **프로젝트에 정식 사본을 복사하는 것을
   개조 절차에 명시**해야 한다. 안 그러면 정상 개조가 실패로 잡힌다.
7. **`templates/legacy-terms.json`** — 기본 블랙리스트가 비더슈탄트 기준이라
   `⚔` 와 `링크아트센터` 가 들어 있다. **FATED 는 둘 다 정상값**이라 오탐이 났다.
   템플릿에 "베이스가 바뀌면 이 파일을 통째로 다시 쓴다"를 크게 적어야 한다.

### 판독·구현

8. **`references/pitfalls.md`** — "작품마다 다른 축" 표에 FATED 열 추가
   (배역 3 / 단층 / edge / 7칸 2단계 / 실물 할인권 **없음** / 전석 단일가).
9. **`references/pitfalls.md`** — 공식 고지 공연시간과 실제 스케줄이 어긋나는 사례(11/18 수요일 2회).
10. **`references/pitfalls.md`** — **블록 단위 삭제는 잘라낸 내용을 단언해야 한다.**
    쿠폰 패널 두 개를 지우다 그 사이에 있던 사진 뷰어가 함께 날아갔다 (§6.2).
    `must_contain` / `must_not_contain` 을 두는 패턴을 싣는다.
11. **`references/pitfalls.md`** — **다크 테마 전용 오버라이드**를 놓치기 쉽다.
    `[data-theme="dark"] .next-show-card` 처럼 라이트 팔레트 밖에 따로 있는 규칙은
    `:root` 만 고치면 남는다. 다크모드 스크린샷이 유일한 발견 수단이었다.
12. **`templates/_shots.html`** — `?theme=dark` 파라미터를 표준으로 넣는다.
    pitfalls §14 가 "다크모드를 확인하라"고 하면서 확인 수단을 주지 않았다.

---

## 10. 기능 확장 제안

수집 데이터로 싸게 붙일 수 있는 것부터. **데이터 보유** 여부를 표시한다.

| 제안 | 데이터 | 비용 |
| --- | --- | --- |
| **첫공 뱃지** — 9명 전원의 첫공 회차를 캘린더에 표시 | ✅ 보유 (공식 표의 `첫공` 뱃지) | 낮음 |
| **27조합 수집판** — 3×3×3 조합 중 본 조합을 격자로 | ✅ 스케줄에서 계산 | 중간 |
| 배우별 관람 횟수 | ✅ 기존 통계에 포함 | 이미 있음 |
| 시놉시스·캐릭터 소개 | ✅ 보유 (SYNOPSIS + 캐릭터 카드 3장) | 낮음 — `guide.html` 에 |
| 특전 캘린더 | ❌ 미공개 | 2차 오픈 후 |

> 27조합 수집판은 3인극이라 성립한다. 다른 작품에 그대로 옮기면 조합이 폭발하므로
> **이번 작품 한정 기능**으로 둘지 검토 부탁드린다.

---

## 11. 베이스 작품 잔재 조사

개조 후 `check_html.py` 를 `index.html` · `guide.html` **양쪽 모두**에 돌린다
(오셀로와 이아고에서 `guide.html` 이 통째로 베이스 작품인 채 L2 를 통과한 적이 있다).

패턴으로 못 잡아 **눈으로 찾아야 할 것** — 숫자·색상·좌표는 어떤 정규식에도 안 걸린다.

- [ ] 이끼숲 팔레트 잔재 (theme-color 메타 포함)
- [ ] 좌석 구역 숫자 `{l:13,m:0,r:13}` · `{l:6,m:10,r:6}`
- [ ] 할인권 사용 기간 날짜 문구
- [ ] `COUPON_PACKS` 의 2026-10-01~10-11
- [ ] `ikki_` / `wider_` 접두사
- [ ] 도장판 명칭 (이끼숲 "아이드 도서카드" → FATED **"MANIA CARD"**)
- [ ] 극장명 (백암아트홀 → 링크아트센터드림 드림2관)
- [ ] 이끼숲 배우명 15명 잔재
- [ ] 모바일 실기기에서 주소창 색 확인
